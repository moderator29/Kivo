# KIVO Build Status

Honest, evidence-based assessment of what exists in the codebase today, checked against the real files rather than assumed from file presence. Written 2026-08-15 as the baseline for the full-integration build pass described in the founder's directive of the same date. Status legend:

- **COMPLETE** — real, working, verified against the actual code.
- **PARTIAL** — real functionality exists but doesn't cover the full scope described.
- **PLACEHOLDER** — a route/component exists but shows a "Coming soon" state or stub, honestly, not fabricated.
- **NOT IMPLEMENTED** — doesn't exist yet.
- **BROKEN** — exists but doesn't work correctly.

This file is a living document — update it as each engine/surface below actually changes, not just when someone claims it changed.

## Authentication & identity

| Area | Status | Notes |
|---|---|---|
| Clerk sign up / sign in / sign out | COMPLETE | `src/app/sign-in/`, `src/app/sign-up/`, Clerk-hosted flows, KIVO-themed via `src/lib/clerk-appearance.ts` |
| Email verification | COMPLETE | Handled entirely by Clerk itself, no KIVO code involved |
| Protected routes | COMPLETE | Resource-level `auth.protect()` per layout (`(app)/layout.tsx`, `admin/layout.tsx`, `onboarding/page.tsx`) — proxy/middleware intentionally does optimistic-only checks, real enforcement is at the resource |
| Clerk → Supabase sync (webhook) | COMPLETE | `src/app/api/webhooks/clerk/route.ts` — svix signature verification, `user.created`/`user.updated`/`user.deleted`, idempotent (duplicate-key on retry is a no-op) |
| OAuth/social sign-in | NOT IMPLEMENTED | Not configured in Clerk yet — this is a Clerk Dashboard config change, not a code change, when the founder wants it |

## Football data & provider abstraction

| Area | Status | Notes |
|---|---|---|
| `FootballDataProvider` interface | COMPLETE | `src/lib/football/types.ts` — real interface, `ApiFootballProvider`, `TheSportsDbProvider`, and dev-only `MockFootballProvider` all implement it |
| API-Football integration | PARTIAL | Fixtures, lineups, events, standings, squads, managers, transfers, H2H are synced. NOT available on the free tier (confirmed, not assumed): injuries, referees, per-match individual player stats, xG on most competitions. Remains the default/primary provider. See `docs/API_FOOTBALL.md`. `syncTodayFixtures` can now be scoped to specific competitions via `FOOTBALL_SYNC_COMPETITION_IDS` (RECOMMENDATIONS.md item 28) — unset by default, so behaviour is unchanged until an operator sets it. |
| Sync diagnostics (`sync_runs.raw_response_sample`) | COMPLETE | RECOMMENDATIONS.md item 65 — migration `0041` adds a bounded (~21K char, check-constraint-enforced), truncated sample of the last raw provider response each provider adapter received, written on every `syncTodayFixtures` finish (success or failure). `FootballDataProvider.getLastRawResponseSample()` on all three providers; honestly `null` for the mock and for TheSportsDB's own success-path status code (its request helper doesn't surface the Response object). |
| TheSportsDB integration | COMPLETE for what it supports, config-selectable | `src/lib/football/providers/thesportsdb.ts` — real v1 REST adapter (fixtures by date/id, standings, squads with real DOB/nationality API-Football's free squads endpoint lacks). Selected via `FOOTBALL_DATA_PROVIDER=thesportsdb` + `THE_SPORTS_DB_API_KEY`; API-Football stays default. Lineups, match events, fixture stats, manager, and transfers are NOT supported by this provider (throws a clear error rather than guessing at an unconfirmed free-tier shape) — see `docs/PROVIDER_ABSTRACTION.md`'s capability matrix. |
| Normalized domain models | COMPLETE | `NormalizedFixture`, `NormalizedTeam`, `NormalizedStandingRow`, etc. in `types.ts` — frontend never sees provider-shaped data, identical across both real providers |
| Quota/rate-limit awareness | COMPLETE for API-Football, N/A for TheSportsDB | `api-football-request.ts` parses `x-ratelimit-requests-remaining`, does one jittered retry on 5xx only, never retries 4xx, and the number IS already surfaced in Admin → Data Health (a live "requests left today" pill, a "quota used today" stat, per-run quota) — confirmed by reading the page, not newly wired this pass. TheSportsDB sends no quota header at all; `getQuotaRemaining()` honestly returns `null`. See `docs/API_QUOTA.md`. |
| Caching by volatility tier | PARTIAL, now documented honestly | Real per-endpoint `fetch` revalidate windows exist for both providers and `last-synced.ts` tracks sync-run-based freshness display — but there is still no formal, enforced TTL-by-volatility-tier *system* (just per-endpoint constants a developer chose). See `docs/CACHING_STRATEGY.md` for the full, honest breakdown. |
| Live polling | CORRECTLY DISABLED, real automated worker now built around it | `FOOTBALL_LIVE_POLLING_ENABLED=false` still gates both the manual admin "Refresh live scores" action and (2026-08-18) the new automated cron worker below — stays this way, unflipped by code, until the founder decides otherwise in Vercel. |
| Supabase Realtime distribution | COMPLETE for what exists today | Migration `0038_realtime_fixture_distribution` publishes `fixtures`/`fixture_events`; `src/hooks/use-realtime-fixtures.ts` + `LiveFixtureList`/`MatchScoreDisplay` push live score/status updates to every viewer the instant any sync writes fresh data — no polling, no page refresh. See `docs/LIVE_DATA.md`. **Fixed 2026-08-17:** `MatchScoreDisplay` was passing a fresh-identity array literal into `useRealtimeFixtures` on every render, which crashed the component with "Too many re-renders" the first time a real live score update landed — the row's live-push claim was not actually reliable in practice until this pass wrapped it in `useMemo`. Also fixed: the score `<span>` now remounts (replays its reveal animation) only on a genuine score change, not on every render. |
| Automated server-side live worker (cron) | **BUILT 2026-08-18** — production firing unverifiable from this sandbox | `src/app/api/cron/sync-live/route.ts`, fired every minute by Vercel Cron (`vercel.json`). Adaptive, not blind: checks `CRON_SECRET` auth, `FOOTBALL_LIVE_POLLING_ENABLED`, dedups against an already-`running` cron run, refuses to spend quota below a floor of 10 remaining requests, and only calls `syncTodayFixtures` when a fixture is actually live/halftime or kicking off within 10 minutes — every decision, including no-ops, is logged to `sync_runs` (`trigger_source: 'cron'`, migration 0044) and surfaced in Admin → Data Health's new "Automated worker" section. Full design and the explicit list of what is/isn't verified: `docs/LIVE_DATA.md`. |
| Data freshness metadata (`fetched_at`/`expires_at`/`data_status`) | PARTIAL | `retrievedAt` exists on fixtures; not yet a uniform pattern across every normalized type |

## KIVO intelligence layer

| Area | Status | Notes |
|---|---|---|
| Rating Engine (`kivoRating` vs `providerRating`) | COMPLETE (engine + tests + docs), NOT wired to UI | `src/lib/football/rating-engine.ts` — position-aware (GK/DEF/MID/FWD via the shared `positionGroup()`), versioned (`RATING_MODEL_VERSION`), `providerRating` typed and always `null` (API-Football's free tier supplies none). Returns `null` per match when a player has no real evidence of involvement (no minutes column exists — see `docs/RATING_ENGINE.md`). 20 unit tests. Deliberately not shown on a real screen yet — uncalibrated against real synced matches; see doc for why. |
| Form Engine (player/team last-5/10/season) | COMPLETE, reusable service | `src/lib/football/form-engine.ts` — generalizes the ad hoc "last 5" logic that used to live only in `teams/[id]/page.tsx`. W/D/L sequence + goals scored/conceded/GD/points for last-5/last-10/season windows, honest `isSufficientSample` below `MIN_FORM_SAMPLE` (2) real finished matches. 11 unit tests. Wired into `players/[id]/page.tsx` ("Recent form" section) and `src/lib/ai/grounding.ts` (favourite team's real form, with an honest "too few matches" line when insufficient). |
| Match Intelligence / `MatchInsights` | COMPLETE, wired into AI Copilot grounding | `src/lib/football/match-intelligence.ts`'s `buildMatchInsights(supabase, fixtureId)` — unifies real H2H (`head-to-head.ts`), both teams' real recent form (`form-engine.ts`, reused not reimplemented), and real goal-timing distribution (`goal-timing.ts`, extracted from the inline logic already in `teams/[id]/page.tsx`, item 162) into one object. Every sub-field carries its own `isSufficientSample` honesty gate. Wired into `src/lib/ai/grounding.ts` for the user's favourite team's most recent finished fixture, so the Copilot can answer "why did [team] lose" from one real, coherent object. 4 unit tests on the pure goal-timing aggregation (DB-touching `buildMatchInsights` itself has no test file, same as `head-to-head.ts`). |
| Heatmap engine | COMPLETE (engine + UI + tests + docs), zero live positional data source | `src/lib/football/heatmap-engine.ts`'s `HeatmapEngine` — pure density-grid builder on a canonical pitch coordinate system matching `PitchLines`'s viewBox, 8 unit tests, zero import from/reference to the removed Sportmonks-tied `premium-stats.ts`. Always receives an empty observation set in production today (no provider connected) — that's the correct, expected state, not a bug. |
| `PositionalDataProvider` interface | COMPLETE, pure seam | `src/lib/football/positional-types.ts` — no current `FootballDataProvider` implements it; not wired to anything live; provider-agnostic by design (no vendor name anywhere in the file). |
| `HeatmapView` UI component | COMPLETE, not yet wired into Match Centre | `src/components/matches/heatmap-view.tsx` — reuses `PitchLines`'s visual language (same as `LineupPitch`); renders a real density overlay when data exists, otherwise a deliberate, polished "Positional data unavailable for this match" empty state. See `docs/HEATMAP_ENGINE.md` for why it isn't on a live tab yet. |
| Fantasy scoring | COMPLETE, documented as "FantasyScoringEngine" | `src/lib/fantasy-scoring.ts` is real and covers goals/assists/cards/clean sheets/captaincy — not rebuilt or renamed this pass (see `docs/FANTASY.md` for why the file path stayed the same); methodology now formally documented. |
| Transfer engine (confirmed vs. rumour) | COMPLETE | Confirmed-only by design — API-Football's free tier has no rumour tier, and `RECOMMENDATIONS.md` item 178 formally retired the idea of inventing a confidence-tier taxonomy. Never regressed. |

## Product surfaces (high level — see RECOMMENDATIONS.md for line-item detail)

| Area | Status |
|---|---|
| Match Centre (tabs, lineups, standings, H2H, fan verdict) | PARTIAL — real, substantial, missing per-player match stats and a formation pitch view landed this session; heatmap still unavailable (honest empty state, not faked) |
| Fantasy (squad builder, gameweeks, leaderboards) | COMPLETE for MVP scope |
| Social (feed, posts, polls, follows, match room) | COMPLETE for MVP scope | Feed now has a real "New posts" live pill via Supabase Realtime on `posts` (migration `0042`, RECOMMENDATIONS.md item 119) — dismissible, click-to-reload, never auto-inserts under the reader's cursor. **2026-08-17:** comment reactions, previously modelled in the `reactions` table (`target_type='comment'`) but never rendered, are now live — `getComments` aggregates real reaction rows and `CommentThread` wires the existing `ReactionPicker` onto each comment. Also fixed: stale feed on All/Following tab switch (`SocialFeed` now remounts per tab), the "New posts" pill was rendering behind the sticky TopBar. **2026-08-18:** Match Room is now genuinely live, per the founder's "live match chat" directive — `use-realtime-room-posts.ts` merges every new post (a fan's own, or a system one) straight into the list, no click required (deliberately different from the feed's own pill; see that hook's doc comment and DECISIONS.md for why). Real goals/red cards now auto-post a system-authored, clearly KIVO-badged announcement into the scoring fixture's Room (RECOMMENDATIONS.md item 254, migration `0047_match_room_system_posts`), and Room's composer is now a lighter, single-line `RoomComposer` instead of the general multi-row `PostComposer`. Moderation (`0045_moderation_status`) and the existing post rate limit were confirmed, live against the project, to already cover Room posts unchanged. |
| AI Copilot | COMPLETE, grounded in real synced data via `buildGroundingContext` | Now also supports a fixture/team/player-scoped `GroundingFocus` deep-linked from Match Centre/team/player pages (RECOMMENDATIONS.md items 184/185), inline provenance chips distinguishing verified provider facts from KIVO-calculated stats in the chat UI (item 188), and a collapsible "what KIVO knows right now" panel showing `grounding.summary` verbatim plus real sync freshness (item 189). |
| Notifications | PARTIAL, real producers now cover most event types | `src/lib/football/match-notifications.ts` wired directly into the sync code paths that already write each real row: match kickoff/full-time (`sync.ts`'s `upsertFixture`, fires on a real, KIVO-observed status transition only — never on a fixture's first-ever sync), goal/red-card (`sync-match-details.ts`'s `upsertFixtureEvent`, fires once on a genuine new `fixture_events` insert, never on re-sync), and any other real event type for a followed player (yellow card, substitution, etc.). Audience is real: `profiles.favourite_team_id` + `follows` for team events, `follows` for player events. **2026-08-17:** `new_follower`, `post_comment`, and `comment_reply` — previously registered in `notification-registry.ts` with no code path that ever inserted them — are now wired: `notifyNewFollower` in `follow-actions.ts` and `notifyComment` in `social/comment-actions.ts` (notifies the post author on a top-level comment, the parent comment's author on a reply, never the commenter themselves). Fantasy gameweek deadline notifications are still NOT implemented. This codebase does now have one real scheduled job (`src/app/api/cron/sync-live/route.ts`, 2026-08-18 — see the "Automated server-side live worker" row above), but it's narrowly scoped to fixture syncing and fires unconditionally on its own minute-by-minute schedule; a gameweek deadline is a different, calendar-driven trigger with no existing detection path, so wiring it would mean genuinely new schedule/detection logic, not a rider on the live-sync worker's minute tick — still explicitly out of scope here. Push notifications also NOT implemented, per standing instruction (no push infra/service worker exists). |
| Search | PARTIAL, recent + popular added | Command palette (`src/components/layout/command-palette.tsx`) now shows recent searches (real queries the user actually completed, localStorage, client-only, no new schema) and "Popular teams" (real `follows` counts via a new narrow SECURITY DEFINER RPC `get_most_followed_teams`, migration 0040 — labelled "Popular", not "Trending", since this codebase has no view-tracking/analytics event log to build a genuine time-windowed trending signal from) on the empty state, alongside the existing quick links. **2026-08-17:** `searchPlatform` (guest-callable, so the app's most exposed query surface) now returns a real `{ error, results }` shape instead of silently collapsing a rate-limit hit into an indistinguishable "No matches"; the palette also gained a proper focus trap (`useFocusTrap`), correct combobox `aria-expanded`/`aria-activedescendant` scoping, and a debounce-window loading state so results never flash "no matches" before a query actually runs. |
| Admin (users, moderation, data health) | PARTIAL — real, but data-quality tooling (duplicate detection, missing-logo detection) doesn't exist yet | **2026-08-17:** the Users table is now explicitly labelled read-only in its own UI ("This table is read-only. Ban, suspend and role-change actions aren't built yet.") rather than silently missing those actions — there is genuinely no `banned`/`suspended` column or RLS policy anywhere in `supabase/migrations` (see RECOMMENDATIONS.md item 234). Users/moderation lists also now show real `count: "exact"` totals when a capped list is actually truncated, instead of truncating silently. |

## Sportmonks

REMOVED this pass, per explicit founder directive, completely — not just the code seam that was already flagged as never-live, but every trace: `src/lib/football/premium-stats.ts` (deleted), the gated "Market" section on the player profile page, the `SPORTMONKS_API_TOKEN` env var everywhere it appeared, and the `players.market_value_eur`/`market_value_updated_at`/`contract_expires_at` columns migration 0036 added (dropped by migration `0039_drop_premium_stats_market_columns.sql`, applied to the live project). `lineups.pitch_heatmap` (also from 0036) was deliberately left in place — it sits inside the heatmap engine work stream's schema surface, not this one's, so dropping it here risked colliding with concurrent, unreviewed work on the same table; see `DECISIONS.md` for the full reasoning. TheSportsDB (see the row above) is the real, implemented replacement second provider — not a stub. See `DECISIONS.md`'s 2026-08-15 "Sportmonks removed entirely" entry for the complete record.
